import { Header } from "../components/Header";
import { Modal } from "../utilities/modals";

export const HomePage = () => {
  return (
    <div>
      <Header></Header>
      <Modal></Modal>
    </div>
  );
};
